{
  "name": "sfb-monorepo",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "node tools/sync-assets.mjs && npm run dev -w sfb-client",
    "dev:server": "npm run build && node packages/server/dist/index.js",
    "test": "vitest run --root packages/shared",
    "build": "node tools/sync-assets.mjs && npm run build -w sfb-client && npm run build -w sfb-server && node tools/build-classic.mjs",
    "start": "node packages/server/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
